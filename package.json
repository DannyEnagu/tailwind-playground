{
  "name": "tailwind-playground",
  "version": "1.0.0",
  "description": "A tailwind playground",
  "main": "index.js",
  "scripts": {
    "build": "tailwindcss build -i input.css -o styles.css",
    "watch": "tailwindcss build -i input.css -o styles.css --watch",
    "dev": "npm run watch",
    "start": "live-server"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  }
}
